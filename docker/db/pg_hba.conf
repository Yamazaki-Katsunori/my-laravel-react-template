# TYPE  DATABASE        USER            ADDRESS                 METHOD

# local socket (Unix domain socket)
local   all             all                                     trust

# local TCP connections from the docker network / host
# 開発: 172.16.0.0/12 のようなネットワークを絞れるなら絞る（docker network による）
# 以下は「どこからでも接続可（パスワード必須）」の開発向け例
host    all             all             0.0.0.0/0               md5

# IPv6 (必要なら)
host    all             all             ::/0                   md5

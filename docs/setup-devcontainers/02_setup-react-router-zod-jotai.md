# react-router/zod/jotaiを利用する場合
`react-router`, `zod`, `jotai` を導入したい場合、下記のコマンドを実行してください。
※ varsion指定は適宜変更してください。
```
cd frontend

pnpm add \
  react-router@^7.9.6 \
  zod@^4.1.12 \
  jotai@^2.15.1

pnpm install
```